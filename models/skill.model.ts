export class Skill {
  name: string;
  logo: string;
  site: string;

  constructor(  
    name: string,
    logo: string,
    site: string,
    ) {
    this.name = name
    this.logo = logo
    this.site = site
  }
}
